# Copy this file to .env and fill in real values. .env is gitignored — never commit it.

# --- Supabase database connection (for the loader) ---
# Use the SESSION POOLER (port 5432), NOT the transaction pooler (6543).
# Supabase dashboard -> Connect -> Session pooler. URL-encode symbols in the password.
SUPABASE_DSN=postgresql://postgres.kfkchlsojixdgmxypqcl:STPQEXm4QRJQfyK0@aws-1-us-east-2.pooler.supabase.com:5432/postgres

# --- Loader data source ---
TWG_SOURCE=local
TWG_LOCAL_DIR=d:\Warren\data

# --- (Future) SFTP source — for pulling directly from the TWG FTP ---
# TWG_SFTP_HOST=dataftp.thewarrengroup.com
# TWG_SFTP_PORT=22
# TWG_SFTP_USER=jey.lcd
# TWG_SFTP_PASS=

# --- Optional loader tuning ---
# TWG_STATEMENT_TIMEOUT=3600
